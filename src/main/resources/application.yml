spring:
  application:
    name: campephilus
  profiles:
    active: prod
  data:
    jpa:
      repositories:
        bootstrap-mode: deferred
    redis:
      repositories:
        enabled: false

server:
  servlet:
    context-path: /campephilus